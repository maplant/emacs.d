;;; custom.el --- -*- lexical-binding: t -*-
;; init.el redirects `custom-file' to a temp file, so this file is
;; intentionally empty. Keep it here so a stale `(load custom-file)'
;; never errors if someone re-adds it.
